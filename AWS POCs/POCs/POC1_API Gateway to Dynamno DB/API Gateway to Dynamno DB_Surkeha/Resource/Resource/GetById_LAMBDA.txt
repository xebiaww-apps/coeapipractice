LAMBDA Code to GET a record by ID:

import json
import boto3
from boto3.dynamodb.conditions import Key

def lambda_handler(event, context):
    
    # TODO implement
    client=boto3.resource('dynamodb')
    table=client.Table('http-crud-tutorial-items')
    subscriber=table.get_item(
    Key = {
        "id" : event["id"]
    })
    print(subscriber['Item'])
    return subscriber['Item']
	
Test Event Configuration:
{
  "id": "xebia14"
}
	